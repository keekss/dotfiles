{
    "name": "Backup Codespace",
    // "build": {
    //     "dockerfile": "Dockerfile",
    //     "context": "."
    // },
    "postCreateCommand": "cat /workspaces/dotfiles/vscode/extensions.json | jq -r '.recommendations[]' | xargs -L 1 code --install-extension"
}